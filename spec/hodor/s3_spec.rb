require 'spec_helper'

describe Hodor::S3 do
  it 'has a version number' do
    expect(Hodor::S3::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
